---
description: "[DEPRECATED] Use the wiki-save skill instead."
category: vault
---

Deprecated - use the `wiki-save` skill.

Run the wiki agent with the `wiki-save` skill:
```
/wiki-save
```

This command is retired as of v0.2. The `wiki-save` skill (owned by the `wiki` agent,
`skills/wiki-save/SKILL.md`) supersedes it with full RBAC enforcement and the v0.2 vault
schema (wiki/entities, wiki/concepts, wiki/synthesis, wiki/sources).
