[project]
name = "second-brain"
version = "0.1.0"
description = "AI-maintained Obsidian second brain — agents, skills, and routing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.0.0",
    "pymupdf4llm>=1.27.2.3",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "websockets>=16.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
